{
  "name": "zurg-serverless",
  "version": "1.0.0",
  "description": "Serverless Real-Debrid WebDAV server for Infuse with STRM files",
  "main": "src/worker.ts",
  "scripts": {
    "dev": "node scripts/wrangler-wrapper.js dev",
    "dev-main": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy-local": "node scripts/wrangler-wrapper.js deploy",
    "test": "wrangler dev --local",
    "types": "wrangler types",
    "setup-kv": "node scripts/smart-kv-setup.js",
    "setup": "npm run setup-kv",
    "setup-dev": "cp .env.example .dev.vars && echo '✅ Created .dev.vars - Edit with your actual values'",
    "check-deploy": "node scripts/check-deploy.js"
  },
  "keywords": [
    "real-debrid",
    "webdav",
    "infuse",
    "strm",
    "cloudflare-workers",
    "serverless"
  ],
  "author": "Zurg Serverless Community",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.3.0",
    "wrangler": "^4.17.0"
  }
}
