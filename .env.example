# Example environment configuration
# Copy this file and set your actual values

# Required: Real-Debrid API token
# Get this from: https://real-debrid.com/apitoken
RD_TOKEN=your_real_debrid_token_here

# Recommended: Fixed IP address for unrestrict calls
# This prevents Real-Debrid account bans from IP address conflicts
# Leave empty to use dynamic Worker IP (not recommended for production)
RD_UNRESTRICT_IP=your_dedicated_ip_address

# Optional: Token for STRM file protection
# If set, STRM files will include ?token=xxx parameter
STRM_TOKEN=your_strm_protection_token

# Optional: Override base URL
# Leave empty for auto-detection from request
BASE_URL=https://your-custom-domain.com

# Optional: Torrent refresh interval in seconds (default: 15)
REFRESH_INTERVAL_SECONDS=15

# Optional: API timeout in seconds (default: 30)
API_TIMEOUT_SECONDS=30

# Optional: Torrents per API page (default: 1000)
TORRENTS_PAGE_SIZE=1000

# Optional: Hide broken torrents in listings (default: true)
HIDE_BROKEN_TORRENTS=true
